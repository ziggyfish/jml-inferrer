(set-info :status unsat)
(set-logic QF_AUFLIA)
(declare-const a (Array Int Int))
(declare-const b (Array Int Int))
(assert (= a b))
(assert (= (select a 0) 1))
(assert (= (select b 0) 2))
(check-sat)
