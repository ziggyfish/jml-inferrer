package com.jml.spec;

public enum Kind {
    REQUIRES,
    ENSURES,
    ASSIGNABLE,
    SIGNALS,
    LOOP_INVARIANT,
    LOOP_DECREASES,
    INVARIANT,
    ALSO_PARTITION,
    AXIOM,
    INITIALLY
}
